#!/bin/bash

# push
# til 루트에서 ./push 를 실행하여 scripts/auto_push.sh 를 간편히 호출하는 스크립트

# 스크립트 디렉토리 기준으로 scripts/auto_push.sh 실행
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
"$SCRIPT_DIR/scripts/auto_push.sh" "$@"
